apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster_argocd.fullname" . }}
  labels:
    {{- include "cluster_argocd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster_argocd.selectorLabels" . | nindent 4 }}
